// examples/demo.ltl — runnable demo for ltl_ini_parser

import std::io
use ltl_ini_parser as lib

fn main() {
    io::println("ltl_ini_parser demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}